<script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=false"></script>
<script type="text/javascript" src="/assets/home.js"></script>

<!-- #TODO: This is a hack to capture latlng which is used immediately in the home view. Move this to welcome screen once we have them. -->
<script type="text/javascript">
    $(document).ready(function() {
        $(initialize());
    });
</script>

<div class="wrapper">
    
        
        <div class="hero-unit">
            <h2>We've found these calendars: </h2>
            <ul>
              <% @calendars.each do |calendar| %>
                <li><a style="color: <%= calendar.foreground_color %>; background-color: <%= calendar.background_color %>"><%= calendar.summary %></a></li>
                  <%= form_for(calendar, :remote => true) do |f| %>
                    <div class="field">
                      <%= f.check_box :skip, :class => 'submittable' %> 
                      <%= f.label "Don't use calendar" %>
                    </div>
                    <!-- <div class="actions">
                      <%#= f.submit %>
                    </div> -->
                  <% end %></li>
              <% end %>
          	</ul>
	          <h2>Let's get your events!<p>
              <%= link_to "Next", event_review_path, :class => "zocial google" %></p></h2>
		        
        </div>

</div><!--/wrapper-->